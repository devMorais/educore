/** US-027: config do provedor LIBRAS vinda do backend (BS-015) — chaves usadas pelo LibrasLoaderService. */
export interface LibrasProviderConfig {
  plugin_url?: string;
  app_url?: string;
  token?: string;
}
